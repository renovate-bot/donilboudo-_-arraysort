import 'package:arraySort/as.dart';

void main() {
 
 List list = [5,1,9,7,0,12,6];
 var sortlist = sort(list);
 print("La liste avant tri est ${list}");
 
 print("La liste apres un tri par ordre croissant est ${sortlist}");
}
